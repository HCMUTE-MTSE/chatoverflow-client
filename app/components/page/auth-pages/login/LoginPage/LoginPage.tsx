import AuthLayout from '../../AuthLayout';
import LoginCard from '../LoginCard';

export default function LoginPage() {
   return (
      <AuthLayout>
         <LoginCard />
      </AuthLayout>
   );
}
